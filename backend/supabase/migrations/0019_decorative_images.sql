-- (Reverted) Decorative per-slug stock covers were removed: random photos that
-- don't depict the actual place/city/route are misleading. Places, cities and
-- routes now fall back to the neutral in-app placeholder; only real event
-- posters (Concert.ua / Resident Advisor) are shown. Intentionally a no-op.
select 1;
